performance:
  collisions: true # if its false disable pocketmine collisions calculation (impacts player grounding)
  block-intersections: true # if its false disable pocketmine calculation of block intersections
  entity-collisions: true # if its false disable pocketmine entity collisions calculation
  falling-blocks: true # if its false disable pocketmine falling blocks calculation